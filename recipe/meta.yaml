{% set name = "samsungtvws" %}
{% set version = "3.0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: c8ea7e03261190066e4d813e34906e593b106cdd8a669afdcf1f9b790f51527b

build:
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  noarch: python

requirements:
  host:
    - python {{ python_min }}
    - pip
  run:
    - python >={{ python_min }}
    - websocket-client >=0.57.0
    - requests >=2.21.0

test:
  imports:
    - samsungtvws
  requires:
    - pip
    - python {{ python_min }}
  commands:
    - pip check

about:
  home: https://github.com/xchwarze/samsung-tv-ws-api
  summary: Samsung Smart TV WS API wrapper
  description: |
    This project is a library for remote controlling Samsung televisions via a TCP/IP connection.
    It currently supports modern (post-2016) TVs with Ethernet or Wi-Fi connectivity. They should be all models with TizenOs.
    Based on https://github.com/marysieek/samsung-tv-api work.
  license: MIT
  license_family: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - mlondschien
    - xchwarze
